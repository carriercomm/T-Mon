[buildout]
extends = base.cfg
parts = instance
        test
        GeoLiteCityDB
        loadtest
        
[settings]
module = testing

[loadtest]
recipe = zc.recipe.egg:script
eggs = rjdj.tmon
scripts = loadtest
arguments = ${settings:module}, *sys.argv
initialization = import sys
 from settings import ${settings:module}
