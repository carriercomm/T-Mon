==============================
 RjDj TMon Server
==============================

# http://www.maxmind.com/app/geolitecity
$ download the GeoLite City DB and extract it into db/

$ py26 bootstrap.py

$ bin/buildout -v

$ bin/initdb

# only once
$ bin/instance syncdb

# only once
$ bin/instance prepare_couch

$ bin/test -vv1


